# nRF Snow Ghost 1.0

**nRF Snow Ghost** — портативный мультипротокольный инструмент для пентестинга и исследований безопасности беспроводных сетей (Wi-Fi, BLE, NRF24, Sub-GHz).  
Прошивка превращает ESP32 в «швейцарский нож» хакера.

---

## ⚠️ Отказ от ответственности

Этот проект создан **только для образовательных целей** и для проверки безопасности **собственных** сетей и устройств.  
Автор **не несёт ответственности** за использование данного ПО в противозаконных целях. Перед использованием убедитесь, что ваши действия соответствуют местному законодательству.

---

## 📦 Необходимые компоненты (BOM)

**Микроконтроллер**
- ESP32 Wroom-32 (DevKit V1) или Wemos D1 Mini ESP32 (рекомендуется для компактности).

**Радиомодули**
- CC1101 (433 MHz) — Sub-GHz.
- NRF24L01+ PA/LNA (с внешней антенной) — MouseJack, Jammer 2.4 GHz.

**Периферия**
- OLED дисплей 0.96" (SSD1306, I2C, 128×64).
- Модуль MicroSD (SPI интерфейс).
- MicroSD карта (до 32 GB, формат FAT32).
- Светодиод WS2812b (NeoPixel) — 1 шт (используется только первый диод).
- Тактовые кнопки — 4 шт.

**Питание и прочее**
- Литий-полимерный аккумулятор (LiPo) 3.7 V.
- Модуль зарядки TP4056.
- Переключатель питания.
- Монтажные провода (рекомендуется силиконовые AWG30 для SPI).
- Электролитический конденсатор 100–470 µF (рекомендуется прямо на радиомодули).

---

## 🔌 Схема подключения (Wiring Diagram)

Все компоненты подключаются к одной плате ESP32. Будьте внимательны: **SPI шина общая** для трёх модулей (SD, CC1101, NRF24). Ниже — сводная таблица пинов и пояснения.

### Общая SPI шина (общая для NRF24, CC1101, SD Card)
- `SCK` → `GPIO 18`
- `MISO` → `GPIO 19`
- `MOSI` → `GPIO 23`

### Chip Select (CS) и управление (индивидуально)
| Модуль     | Сигнал         | GPIO ESP32 | Примечание                         |
|------------|----------------|------------|------------------------------------|
| SD Card    | CS             | `GPIO 13`  | Chip Select                        |
| NRF24L01   | CSN            | `GPIO 17`  | Chip Select                        |
| NRF24L01   | CE             | `GPIO 5`   | Chip Enable                         |
| CC1101     | CSN (CS)       | `GPIO 27`  | Chip Select                        |
| CC1101     | GDO0           | `GPIO 15`  | **Используется для TX/RX таймингов**|
| CC1101     | GDO2           | —          | Не используется                    |

> **ВАЖНО:** GDO0 от CC1101 должен быть подключён к `GPIO 15`. Прошивка использует этот пин для аппаратного захвата/передачи через RMT драйвер ESP32. При неверном подключении Sub-GHz функции не будут работать.

### Дисплей и индикация
| Модуль | Сигнал | GPIO ESP32 |
|--------|--------|------------|
| OLED   | SDA    | `GPIO 21`  |
| OLED   | SCL    | `GPIO 22`  |
| NeoPixel | DIN  | `GPIO 14`  |

### Кнопки (используется `INPUT_PULLUP`, кнопки замыкаются на GND)
| Кнопка  | Функция          | GPIO ESP32 |
|---------|------------------|------------|
| UP      | Вверх            | `GPIO 26`  |
| DOWN    | Вниз             | `GPIO 32`  |
| SELECT  | Выбор / OK       | `GPIO 33`  |
| BACK    | Назад / Стоп     | `GPIO 25`  |

---

## 💡 Тонкости сборки и советы

### Питание 3.3V
- NRF24L01+ (PA/LNA) и CC1101 имеют большие пиковые токи.  
- **Обязательно** припаяйте электролитический конденсатор (100–470 µF) между VCC и GND прямо на ножки радиомодулей. Без этого NRF24 может работать нестабильно.

### Длина проводов SPI
- Шина SPI работает на высокой частоте — держите провода короткими (рекомендуемо ≤ 10 см).

### Размещение антенн
- Разносите Wi-Fi антенну (ESP32) и внешние антенны (NRF/CC1101) как можно дальше друг от друга, чтобы уменьшить взаимные помехи.

---

## 🚦 Светодиодная индикация (LED Status)

RGB светодиод (NeoPixel) отображает статус без необходимости смотреть на экран.

**Основные режимы**
- 🔵 **Синий (Static)** — Режим ожидания (IDLE) / Меню  
- 🟢 **Зелёный (Blink)** — Сканирование Wi-Fi (Scan)  
- 🔴 **Красный (Strobe)** — Атака Wi-Fi Deauth или глушение NRF24  
- 🟠 **Оранжевый (Blink)** — Beacon Spam (фейковые сети)  
- 🟣 **Фиолетовый (Solid)** — Evil Twin (запущен Web-сервер)  
- 🌸 **Розовый (Blink)** — MouseJack (BadUSB инъекция)  
- 🔵 **Синий (Breathing)** — BLE Spoofing (спам на телефоны)

**Специальные события**
- 🌈 **Rainbow (радуга)** — Успех! Перехвачено Wi-Fi handshake  
- ⚪ **Белая вспышка** — Детекция Sub-GHz сигнала  
- 🔴 **Красный (Solid)** — Системная ошибка (например, нет SD карты)

**Stealth Mode:** удержать кнопку `SELECT` > 2 сек в меню — полностью отключить светодиод.

---

## 🎮 Руководство пользователя (UI)

**Навигация**
- UP / DOWN — навигация
- SELECT — выбор / подтверждение
- BACK — назад / стоп

**Основные пункты меню**
- **WiFi Scan** — поиск сетей 2.4 GHz. После сканирования можно выбрать сеть.
- **WiFi Deauth** — отправка deauth пакетов клиентам выбранной сети (нужна цель).
- **Beacon Spam** — создание множества фейковых сетей (например: `FBI Van`, `Free WiFi`).
- **Evil Twin** — клонирование SSID выбранной сети и создание открытой точки доступа; пароли сохраняются на SD.
- **BLE Spoofer** — спам сообщений подключения (вызовет лаги на iOS/Android). Типы: iOS, Android, Windows.
- **NRF Jammer** — глушение сигнала беспроводных мышек.
- **Sub-GHz RX** — приёмник / анализатор 433 MHz.
- **Sub-GHz TX** — воспроизведение/реплей сохранённых сигналов.
- **Admin Panel (Web)** — управление через телефон (см. ниже).

---

## 🌐 Web Admin Panel

- В меню выбрать **Admin Panel**.  
- Подключиться к Wi-Fi:  
  - SSID: `nRF_Admin`  
  - Пароль: `ghost1234`  
- Открыть в браузере: `http://192.168.4.1`

Через панель можно:
- скачивать перехваченные `.pcap` файлы,
- просматривать логи,
- управлять записями Sub-GHz и т.д.

---

## 🚀 Установка прошивки

**Требования**
- VS Code + PlatformIO (рекомендуется) или Arduino IDE.
- Установленные драйверы CP210x или CH340 (в зависимости от платы).

**Настройка в Arduino IDE**
1. Установить поддержку плат ESP32 (Boards Manager).
2. Выбрать плату: `ESP32 Dev Module`.
3. Partition Scheme: **Huge APP (3MB No OTA / 1MB SPIFFS)** — *критически важно*, прошивка не влезет в стандартную разметку.

**Библиотеки (установить через Library Manager)**
- Adafruit NeoPixel  
- U8g2  
- RadioLib  
- ArduinoJson

**Загрузка**
- Открыть `nRF_Ghost.ino` (или скопировать исходники в папку скетча) → Upload.

---

## 💾 Структура SD карты

- Формат: **FAT32**
- Рекомендуемые файлы/папки:
  - `/settings.json` — настройки (может создаваться автоматически)
  - `/scripts/` — скрипты для Sub-GHz (опционально)
  - `/captures/` — для `.pcap` (если используется)

---

## 🐛 Известные ограничения v7.4

- **WiFi Deauth**: не работает против WPA3 и сетей с включённым PMF (Protected Management Frames).
- **NRF MouseJack**: работает только с устаревшими/уязвимыми донглами (без шифрования).
- **Sub-GHz Replay**: длинные сигналы (> 3 сек) могут вызывать перезагрузку устройства из-за нехватки RAM — рекомендуется записывать короткие сигналы (шлагбаумы, звонки и т.п.).

---
